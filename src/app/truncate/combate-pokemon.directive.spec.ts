import { CombatePokemonDirective } from './combate-pokemon.directive';

describe('CombatePokemonDirective', () => {
  it('should create an instance', () => {
    const directive = new CombatePokemonDirective();
    expect(directive).toBeTruthy();
  });
});

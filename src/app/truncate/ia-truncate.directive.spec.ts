import { IATruncateDirective } from './ia-truncate.directive';

describe('IATruncateDirective', () => {
  it('should create an instance', () => {
    const directive = new IATruncateDirective();
    expect(directive).toBeTruthy();
  });
});
